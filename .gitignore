# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# GitReins sandbox (evaluation scratch space)
.gitreins-sandbox/
# GitReins guard history (runtime artifacts, no versioning needed)
.gitreins/history/
# Runtime task data (committed separately on gitreins branch)
.gitreins/tasks.yaml

# Secrets
.env
*.key
*.pem

# Test artifacts
.coverage
.skylos/
# Node (if present from bootstrap)
node_modules/
apps/
packages/
temporal-vector/
# Temp
test_write
*.tmp
*.db

# Cross-project artifacts (not part of GitReins)
category-mapping.yaml
hivemind-audit.md

# GitReins nested runtime artifacts
.gitreins/.gitreins/
.gitreins/config.yaml.bak

# Hilo — local cache files (rebuildable)
.vfs/graph/graph.db
.vfs/graph/graph.db.wal
.vfs/graph/.last_warm

# Demo nested git dirs (demo projects have their own git repos)
demo-calc/.git/
demo-calc/calc/.git/
demo-slugify/.git/
