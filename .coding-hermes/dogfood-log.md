# Dogfood Log — gitreins-poc

| Date | Verdict | Promise | Top findings | Time-to-first-success |
|---|---|---|---|---|
| 2026-08-03 | 🟡 PROMISING-BUT-ROUGH | "Install gitreins into any repo, define criteria tasks, get agentic per-criterion judging, and commit through Tier-1 guards that block secrets" | (1) P0 `gitreins init` generates gitleaks config with invalid regexes → secrets guard permanently fails when gitleaks installed; (2) P1 default `test_command: pytest` can't import root package (pytest 9) → tests guard blocks commits that pass via `python3 -m pytest`; (3) P1 MCP judge times out at 300s on repos whose suite >5 min (full suite here ~11 min) | ~20 min to first green guard (after 2 config workarounds); judge on tiny repo: 76 s |

Details: `docs/dogfood/2026-08-03-integration.md` (real-use report), `docs/dogfood/diagnostics.md` (build/error trail), `skills/gitreins-usage/SKILL.md` (agent usage skill). Board tasks: DF-001..DF-006.

| 2026-08-14 | 🔴 DOES-NOT-DELIVER (PyPI release) / 🟡 PROMISING-BUT-ROUGH (repo HEAD) | "`pip install gitreins`, install/init in any repo, criteria tasks, agentic per-criterion judge, Tier-1 guards that block secrets on commit" | (1) P0 release lag: PyPI 0.11.0 (07-23) predates the DF-001 gitleaks-regex fix (08-03) — fresh install's first `gitreins guard` fails `✗ secrets — ○` (gitleaks panic on bare `*.log` allowlist globs); 201 commits/166 fixes unreleased, no release pipeline in CI (DF-010); (2) P1 pre-commit hook calls bare `gitreins` → PATH shadowing ran gitreins 0.8.1 → real sk-/ghp_ secrets committed through a "PASSING" hook (DF-011); (3) P1 gitleaks reports "no leaks found" for sk-/ghp_ patterns the built-in scanner catches (DF-012); GR-099 correctly blocked but re-verified 13 days straight (DF-013) | PyPI path: NO first success (guard broken from first run); repo-HEAD path: ~10 min to green guard + working LLM judge |
| 2026-08-27 | 🟡 PROMISING-BUT-ROUGH (PyPI 0.12.0 fresh-install path) | "`pip install gitreins` (0.12.0), install/init, criteria tasks, per-criterion agentic judge, guards that block secrets on commit" | (1) P0 wheel version-drift: `gitreins --version` says 0.11.0 on 0.12.0 code (wheel engine/version.py stale) → "Update available: 0.11.0 → 0.12.0" on EVERY command (DF-015); (2) P1 secrets scan reports only the FIRST finding per file — sk- masks ghp_ in the same file (DF-016); (3) P1 DF-002 regression on the uv path: init writes `uv run pytest`, root-package layout → ModuleNotFoundError, guard FAILs with zero detail (DF-017) | ~4 min to first green guard (incl. 2 real code bugs the harness caught + 1 pytest-layout fix); judge on tiny repo: ~3.5 min |

Details: `docs/dogfood/2026-08-27-integration.md` (real-use report), `docs/dogfood/diagnostics.md` (08-27 section). Board tasks: DF-015..DF-019.
2026-09-07 | PROMISING-BUT-ROUGH | 15s t2fs | friction 8 | 5 findings\n
| 2026-09-15 | 🟡 PROMISING-BUT-ROUGH | "pip install gitreins, install/init in any repo, criteria tasks, per-criterion agentic judge, guards that block secrets on commit" | (1) P1 task complete FAILS in fresh consumer envs — init dirties config.yaml → full-suite safety trigger → tests/test_lsp.py FAILS (not skips) without pylsp → Overall FAIL although criteria + judge PASS (DF-GITREINS-POC-6; repro 1 failed/824 passed, verdict evidence docs/dogfood/evidence/); (2) P1 PyPI 0.12.1 (08-28) is ~70 commits behind HEAD — wheel still ships the POC-3 init mismatch and has NO worktree command; release pipeline (DF-010) still missing (DF-GITREINS-POC-7); (3) P2 tier1 failure evidence in task complete is 500 head-chars of pytest banner — failing test name never shown (DF-GITREINS-POC-8) | Leg A fresh machine (las-bunker-04 agent): ~5 min via uv (README pip path impossible: no pip/venv/sudo); Leg B HEAD consumer: ~10 min to green guard, full judged loop 32s judge / 35min with POC-6 diagnosis; regression checks: DF-011/DF-015/DF-016 fixes verified on wheel, POC-1/2/3 at HEAD, POC-3+worktree missing from wheel

Details: `docs/dogfood/2026-09-15-integration.md` (two-leg real-use report),
`docs/dogfood/diagnostics.md` (09-15 section: pylsp trap diagnosis, evidence truncation,
wheel-vs-HEAD proof), `docs/dogfood/evidence/` (verdict.json + fullsuite log),
`skills/gitreins-usage/SKILL.md` v1.1.0 (worktree section, pitfalls 14–17).
Board tasks: DF-GITREINS-POC-6, -7, -8 (pending).
Install leg: SKIPPED-install-bunker on las-bunker-03 (host down, ssh timeout) —
RUN on las-bunker-04 instead (bunker agent 08e03826, spawned+destroyed cleanly).
2026-09-16 | PROMISING-BUT-ROUGH | 82s t2fs | friction 14 | 5 findings

| 2026-09-16b (qa) | 🟡 PROMISING-BUT-ROUGH | "pip install gitreins (0.13.0 wheel, shipped 05:41Z today), install/init in any repo, criteria tasks, per-criterion agentic judge, guards that block secrets on commit" | (1) P1 judge tier1 runs secrets-only — no tests/lint; guard and judge disagree on the same tree; POC-12 NOT fixed (09-15 closure confounded by untracked secrets in the scratch repo; judge scans worktree, guard scans staged); (2) P3 guard secrets verdict doesn't name the scanner — gitleaks vs built-in fallback coverage differs per machine; (3) wheel regression battery green: POC-16/POC-13/POC-3D/POC-10/DF-011/DF-015 all verified FIXED on 0.13.0, anti-tamper canary blocked by control hook — first canary (malformed 33-char ghp_) passed and briefly fabricated a P0, killed by direct gitleaks A/B | 13 s to working CLI (venv path on bunker agent; README literal pip path blocked by PEP-668)

| 2026-09-20 | 🟡 PROMISING-BUT-ROUGH | "An AI agent connects gitreins mcp-server (12 tools), manages criteria tasks, dispatches background LLM evals, polls them, commits through the harness — and everything is browsable via gitreins serve" — surfaces NEVER touched by runs 1-6 (CLI/guard/judge/PyPI all swept before) | (1) P1 MCP async-judge verdicts are never persisted to .gitreins/history — job record lives only in ~/.local/share/gitreins/jobs/, so gitreins serve/report show NOTHING for MCP-driven runs (the agent-primary surface has no audit trail) (DF-GITREINS-POC-23); (2) P2 judge.status has no termination field — a client polling until running==false loops forever (POC-24); (3) P2 harness: bunker-qa.sh launch failed 3 ways, all reported as success (tool-cap kill mid-sync; CAP_KB unbound ships 0-byte qa-run.sh; no liveness probe) (DF-GITREINS-POC-25). Regression sweeps GREEN: protocol negotiation, 12-tool surface vs docs, commit-block while in_progress, commit keeps all evaluated files (09-07 P0 does not reproduce), PyPI wheel == HEAD at 0.14.0, fresh-agent install 16s | ~20 min to a working raw JSON-RPC client end-to-end; judge eval ~7 min (deepseek-v4-flash); serve browser live at 167 verdicts |

Details: `docs/dogfood/2026-09-20-integration.md` (real-use report), `docs/dogfood/diagnostics.md`
(09-20 section: MCP wiring tour, async-job resume, POC-23/24 anatomy, serve safety model),
`skills/gitreins-usage/SKILL.md` v1.3.0 (MCP client section). Board rows: DF-GITREINS-POC-23, -24, -25.
Install leg: RUN by hand on las-bunker-02 agent 70bc1d49 (spawned+destroyed cleanly) after three
bunker-qa.sh launch failures — 16s PyPI install, smoke clean except known fresh-venv pytest gap.

| 2026-09-23 | 🟡 PROMISING-BUT-ROUGH | "A foreman can ask the repo a question (gitreins resolve) and get a calibrated RESOLVED/REVIEW/UNRESOLVED verdict banded in code inside a measured token budget, and can preflight a board row and get a dispatch decision" — the v0.15.0 resolution gate, untouched by runs 1-6 | (1) P1 POC-35: every surface ships disabled, undocumented; fix hint cites a nonexistent §9; this repo's own config never enabled it — a fresh user's first flagship call fails in 0.12s with surface-disabled; (2) P1 POC-36: resolution verdicts never persist (no history entry, no usage.jsonl line, invisible to report/serve) — same class as POC-23 and against the spec's own §8 acceptance; (3) P2 POC-37: preflight --json embeds verdict_json as a string, no top-level verdict field, dual shape vs resolve | Regression facts GREEN: real discrimination on real premises (0.85 true vs 0.29 open vs 0.05 nonsense), budget law enforced (--budget 2000 → 1963 est tokens, 1-file manifest, verdict IMPROVED to 0.92 — precision beats volume), truncation always disclosed (clipped + chars_dropped), fail-closed ABSTAIN exit 1 with named reason + action, preflight fail-open dispatch as documented, MCP context.resolve live (13 tools, protocol clean), exit codes exactly as documented, estimator conservative (within 7% of actual input tokens) | ~6 min to first success (once enabled); friction 2 real items after enable; perf: preflight 2.297s±0.058 warm / 2.752s±0.067 cold, resolve 2.269s±0.052 warm — nothing a user feels, no PERF row filed. Verdict: 🟡 PROMISING-BUT-ROUGH — the engine earns ✅ on every measured dimension, but the box it ships in (config off by default + undocumented, verdicts evaporating) makes the flagship invisible to anyone who has not read the engine source.

Details: docs/dogfood/2026-09-23-integration.md (probe table, working config, perf numbers),
docs/dogfood/diagnostics.md (09-23 section), skills/gitreins-usage/SKILL.md v1.5.0
(resolution gate usage + pitfalls 26-28). Board rows: DF-GITREINS-POC-35 (P1), -36 (P1),
-37 (P2) — appended via /tmp script, board 325→328 rows, tail verified.
Install leg: RUN via bunker-qa.sh on bunker-las-02 (agent dedef735, ttl 4h) — battery complete, 16 cells: fresh-install OK (gitreins 0.15.0 installed from source on bare Debian with bootstrapped venv/zig-cc/make toolchain), native suite PASS incl. under a 3G memory cap, toolchain-bootstrap 5x OK, upgrade 0.14.0→HEAD clean, ci-pass OK via the harness's native-suite fallback (act itself rc=1 inside its docker env — environment, not code; the native suite is the designed fallback), docker-deploy/chaos-shutdown N/A (no compose file), chaos-corruption N/A (no db files), ui-probe INFO (CLI, no server), chaos-errorpath INFO (clean exit on missing config), chaos-disconnect INFO (no network dep), chaos-resource PASS. Agent destroyed, evidence pulled and verified (16 rows, /tmp/dogfood-gitreins/bunker-qa-evidence.jsonl). Foreman not woken, cooldowns untouched per the 2026-09-09 fleet law. No code changed.

| 2026-09-23b | 🟡 PROMISING-BUT-ROUGH | "A user can opt in to a Tier-1 guard that localizes known CVEs against staged Python code (README's documented config block), tunable by min_confidence and cve_source, never blocking on missing infrastructure" — the security-scan guard, never exercised by runs 1-7 | (1) P1 POC-38 config-home split: README documents `defaults.security_scan.enabled` but the guard reads `guards.security_scan.enabled` (guard_manager.py:950) — following the README verbatim yields `Tier 1 Guards: PASS` with NO security_scan line (silent no-op; proven both shapes side-by-side); (2) P1 POC-39 `min_confidence` filters only the CVE feed, never scanner findings (hard-coded conf 0.0 + guard fails on ANY finding) → the word "injection" in a COMMENT fails a commit at the documented 0.7; (3) P2 POC-40 force-ml dep hint names only huggingface_hub (transformers also required) and guard-path config keys `model:`/`cve_source:` are read by nothing | Heuristic pipeline itself: ~8 min to first finding (write vulnerable file → stage → scan); exit codes 0/1/2 exactly as documented; text/json agree; guard reproduces end-to-end on the bunker fresh box. Perf: 0.096s warm — no PERF row. Verdict: 🟡 PROMISING-BUT-ROUGH — the scanner itself is genuinely fine and fast; the box it ships in (dead documented config home + no-op tuning knob) means the documented enablement path silently runs nothing.

Details: `docs/dogfood/2026-09-23b-integration.md` (probe table S1-S9, the working config shape, integration example), `docs/dogfood/diagnostics.md` (09-23b section:
why the split exists — CLI reads defaults, guard reads guards, added in different GR-117 waves). Board rows DF-GITREINS-POC-38 (P1), -39 (P1), -40 (P2) —
appended to .coding-hermes/tasks.md run-8 section, verified.
Install leg: RUN on bunker-las-03 (agent a8015da1, ttl 2h): clone OK with existing
access, 20s venv install, guard reproduced the finding on the fresh box, agent
DESTROYED and verified gone (list grep = 0). Foreman not woken, cooldowns untouched
per the 2026-09-09 fleet law. No code changed. | "A team or agent fleet can record what its QA runs actually did — harness-run or outside the harness — into one browsable ledger, gate commits on a message audit, and self-verify in disposable worktrees without a bunker" — surfaces NEVER touched by runs 1-6 (QA ledger, commit-msg audit, disposable batteries in a NON-fleet repo) | (1) P1 `worktree fresh|repro|dogfood` refuse to run in any repo lacking `.coding-hermes/board/` — the fleet scheduler's layout, undocumented as a prerequisite; `mkdir -p` makes the identical command pass in 0.19s, and the infra failure is reported as exit 1 instead of the documented 2, as a raw traceback (DF-GITREINS-POC-27); (2) P1 fresh-venv install: `gitreins guard` from an UNACTIVATED venv fails `tests (full) — /bin/sh: 1: pytest: not found` even with pytest installed INTO that venv, and the README's own "Try the hook" first commit is BLOCKED (exit 1); `source .venv/bin/activate` fixes it (POC-28); (3) P1 `qa record` with neither --verdict nor --exit-code writes `verdict: UNKNOWN` exit 0 although docs promise a passing verdict, and `--evidence` accepts a nonexistent path (POC-29). Plus P1 POC-30 (`commit-audit` is a silent no-op with stdout AND stderr empty unless a pipeline stage is hand-written, and only a TOP-LEVEL `commit_audit.mode: block` actually blocks — stage-level `mode: block` is dead config), P2 POC-31 (`install` omits `.gitreins/qa-ledger.jsonl` from the consumer `.gitignore`, so the next `git add -A` commits fleet QA rows), P2 POC-32 (rotation at `max_entries` is silent: record exits 0, count unchanged). Regression sweeps GREEN: PyPI wheel == HEAD 0.14.0, DF-011 hook path pinning, QA-ledger fleet-schema interop, no commit-msg hook installed by install/init (matches docs) | ~3 min to a working QA ledger; ~20 min to a working message audit; friction count 9 (7 inside the documented flow) |

Details: `docs/dogfood/2026-09-20b-integration.md` (real-use report: working flow, the 9-item
error table, the fresh-machine leg), `docs/dogfood/diagnostics.md` (09-20b section: why each
silent no-op happens — `engine/pipeline.py:253` empty stage list, `_load_commit_audit_config`
top-level-only `mode`, `engine/repo_paths.py:264-269` board gate), `skills/gitreins-usage/SKILL.md`
v1.4.0 (commit-msg audit section, board prerequisite, pitfalls 21-25). Board rows:
DF-GITREINS-POC-27..-32 (6 rows: 4xP1, 2xP2).
Install leg: RUN on las-bunker-03 (host UP this time; agent 3f4f7cdc spawned, used, destroyed and
verified gone) — 32s venv install of the 0.14.0 wheel; all three P1s reproduce on the wheel.
Foreman not woken, cooldowns untouched per the 2026-09-09 fleet law. No code changed.
