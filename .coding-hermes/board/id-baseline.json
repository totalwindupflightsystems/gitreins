{
  "comment": "Legacy duplicate ids grandfathered by the board id gate (QA-GITREINS-POC-8). Counts are asserted by scripts/check_board_ids.py, so the baseline can only shrink deliberately, in the same commit as the board change that makes an entry stale. 2026-09-20 (tick 316): the QA-GITREINS-POC-1/2/3 entries were dropped here because the duplicate-id collapse commit that landed in this push's ancestry removed every duplicate row, making those entries stale; the producer-side fix in hermes-dagger examples/coding-hermes/qa.ts remains the reason the baseline existed at all.",
  "duplicate_ids": {}
}
