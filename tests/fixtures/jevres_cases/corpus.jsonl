{"id": "resolved-payments-negative-rejection", "category": "resolved", "question": "Confirm that this tree rejects a negative charge amount before any fee lookup, and that a check in the same tree exercises that rejection.", "workdir": "payments_fixed", "seeds": ["validator.py", "validator_checks.py"], "expect_missing_kind": "none", "mock_noul": 0.97, "note": "Ground truth is physical: validator.py raises ValidationError for amount_cents <= 0 before fee_lookup, and validator_checks.py exercises the refusal. Label verified against the tree files, not against any resolver output."}
{"id": "unresolved-payments-negative-rejection", "category": "unresolved", "question": "Confirm that this tree rejects a negative charge amount before any fee lookup, and that a check in the same tree exercises that rejection.", "workdir": "payments_vulnerable", "seeds": ["validator.py"], "expect_missing_kind": "implementation", "mock_noul": 0.59, "note": "The fixed-state half of the resolved/unresolved PAIR above: identical question, pre-fix tree. validate_amount is a live-validated calibration finding: phrased as an information question ('Does this tree reject…?') jev-1.13 scored this UNRESOLVED tree 0.95 RESOLVED (decidability, not confirmation); phrased as confirmation it scores 0.48 UNRESOLVED / implementation (live, 2026-09-21, typesafe/jev-1.13-20260917). Pre-dispatch gates must ask confirmation questions."}
{"id": "resolved-quotegate-window", "category": "resolved", "question": "Confirm that this tree refuses the sixth call inside a rolling 60-second quote window, and that a check in the same tree exercises that refusal.", "workdir": "quotegate", "seeds": ["limiter.py", "limiter_checks.py"], "expect_missing_kind": "none", "mock_noul": 0.94, "note": "RateLimiter.allow returns False once limit hits land inside the 60s window and reopens after it rolls; limiter_checks.py drives exactly that sequence."}
{"id": "unresolved-quotegate-window", "category": "unresolved", "question": "Confirm that this tree refuses the sixth call inside a rolling 60-second quote window, and that a check in the same tree exercises that refusal.", "workdir": "quotegate_missing", "seeds": ["README.md", "config.py"], "expect_missing_kind": "implementation", "mock_noul": 0.81, "note": "The tree's only code is a constants module. The rolling window is DESCRIBED in the README as planned — a mentions-based bundle that reads RESOLVED here is exactly the false positive this corpus exists to catch."}
{"id": "near-miss-payments-positive-validation-mentions", "category": "near_miss_negative", "question": "Confirm that this tree rejects a negative charge amount before any fee lookup, and that a check in the same tree exercises that rejection.", "workdir": "payments_mentions", "seeds": ["validator.py"], "expect_missing_kind": "implementation", "mock_noul": 0.56, "note": "The module is ABOUT amount validation and the words 'negative amounts are rejected' appear verbatim — but no check exists. A mentions-driven noul sits HIGH (this is the danger zone, just under RESOLVED); false positive on quoted/mentioned content is the fleet's documented calibration failure mode."}
{"id": "near-miss-payments-positive-validation-unwired", "category": "near_miss_negative", "question": "Confirm that this tree rejects a negative charge amount before any fee lookup, and that a check in the same tree exercises that rejection.", "workdir": "payments_unwired", "seeds": ["runner.py", "validator.py"], "expect_missing_kind": "wiring", "mock_noul": 0.38, "note": "The check EXISTS (runner.py validate_amount raises for <= 0) and validator.py ships ValidationError — but the capture entry point calls fee_lookup directly and never routes through it. Implemented-but-unwired is a distinct near-miss kind, and its noul is the highest of the negatives: the code genuinely is there."}
{"id": "near-miss-quotegate-rolling-window-untested", "category": "near_miss_negative", "question": "Confirm that this tree refuses the sixth call inside a rolling 60-second quote window, and that a check in the same tree exercises that refusal.", "workdir": "quotegate_untested", "seeds": ["limiter.py", "limiter_meta.py"], "expect_missing_kind": "test", "mock_noul": 0.58, "note": "The limiter is fully implemented AND wired (QuoteAPI uses it) — but the only test in the tree asserts the limit constant, never the refusal. A test that exists but does not cover the behaviour must not read as resolved; the question demands the check too."}
{"id": "budget-starved-quarantine-report-full-ledger", "category": "budget_starved", "question": "For each entry in QUARANTINE_LEDGER, summarise the date, the component, the ruling, the named reviewer and the stated reason — all 1200 entries, one line each in recorded order — and map every reviewer to their team from REVIEWER_TEAMS.", "workdir": "payments_ledger", "seeds": ["incident_ledger.py", "reviewers.py"], "expect_surface": "budget_exhausted", "expect_missing_kind": null, "mock_noul": 0.13, "note": "A genuine answer (1200 per-entry lines plus the reviewer map) cannot fit the enforced 28k-token ceiling: the ledger alone is ~124k chars (~35.5k tokens at the densest measured rate), so it clips to the budget and reviewers.py is dropped by name — budget_exhausted=true with the disclosure — or the worst-case wall ABSTAINs with reason budget-exhausted. It must NEVER surface as an UNRESOLVED band, which would misread 'no room' as 'no code'."}
