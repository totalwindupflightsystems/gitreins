"""Static configuration only — no rate limiting lives here (JEVRES-005 corpus).

Ground truth for ``unresolved-quotegate-window``: the tree's only code is this
constants module; the planned quote gate does not exist.
"""

MAX_QUOTE_AMOUNT_CENTS = 500_000
DEFAULT_QUOTE_TTL_SECONDS = 30
